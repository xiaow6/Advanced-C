1. (float)(25 / 10) = 2.0
(float) 25 / 10 = 2.5
2. 乘法运算必须在加法运算前完成，但并没有规则规定函数调用完成的顺序。
3. 用在配置寄存器上，底层代码经常会用。
4. 没有区别
5.(leap_year % 4 == 0 && leap_year % 100 != 0 || leap_year % 400 == 0)?leap_year = 1: leap_year = 0
6.如何定义副作用？
++ -- 都会修改使用它的值，= 以及各种+= -=都会修改左边的值。
7. Inrange，1 <= a <= 10 其实是1 <= a等于1，然后1 < = 10
8. 
for (a= f1(x); b = f2(x + a), c = f3(a, b), c > 0; a = f1(++x)){statement;}
9. 不要混用布尔值和普通int，所以失败，如果加起来刚好是0.
10. 11，略
12.打印出来啊，负数右移打印出来是负数就是算数，是正数就是逻辑